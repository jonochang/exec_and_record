[package]
name = "exec_and_record"
version = "0.2.2"
edition = "2021"
license = "MIT OR Apache-2.0"

[dependencies]
anyhow = "1.0"
atty = "0.2"
chrono = { version = "0.4", default-features = false, features = ["clock"] }
clap = { version = "4.5", features = ["derive"] }
shell-escape = "0.1"
